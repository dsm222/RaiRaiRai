## codebase-memory-mcp

项目索引：`RaiRaiRai`，根目录 `C:\Users\dsm\Desktop\RaiRaiRai`，使用 `full`，只包含本项目代码和文档。
UE5.8 源码：`C:\Program Files\Epic Games\UE_5.8\Engine\Source`；查引擎源码前先读取 `C:\Program Files\Epic Games\UE_5.8\Engine\Source\AGENTS.md`，不要混入项目索引。



这是一个独立游戏：“显卡制造商模拟器” 前期类似于超市模拟器，玩家开局手搓显卡来卖，然后用钱升级科技 后期类似于X4基石，没有流水线的异星工厂

刚开始自己是一个专家，花本金购买了机器，手动制作显卡

制作完可以在网上卖，或者在实体店卖

网上卖的快递员会来取货

显卡需要多个源（例如GPU，VRAM，PCB），最开始的时候，没办法自己制作，只能买，朋友赞助了仓库的好多

等玩家在工作台把这些拼凑好，全部卖完之后，就有很大的本金了

手动研究显卡，加上物理模拟功能，做成一个有意思的小游戏

之后的拼凑，也可以这样，用各种接口拼凑，自己制作研究出来牛逼的显卡，更牛逼的参数使得大卖

后期资金大了，可以开分店，店和工厂分离

之后就可以去自己生产源"



项目架构：
现在才刚开始，游戏的架构还不明确，但大概的思路是

代码大部分用vibecoding，所以要遵循tdd来实现

大概的思路就是，分为三层  
数据层，存储状态等信息  
行动，逻辑层：负责用数据执行逻辑规则  
处理层：相当于游戏前端，调用到逻辑层

也就是类似于ECS，或者mvc架构（你在具体想实现方案的时候，也可以参考这两个架构）

与传统不同的是，处理层不止可以用游戏画面表现，也可以用脚本或者代码来操控  
这样做的好处是，让游戏也可以实现tdd，你在编写测试代码的时候，可以直接模拟已有的状态，然后经过脚本模拟实际的操作，看看最终结果是否正确，这样就可以稳定的写出可用代码，并且可以让你自己debug

具体的架构需要我们在写代码的时候具体讨论，目前只是说明我的思路



一个功能通常分为两个阶段处理：

1. 第一次大规模实现：按照用户给出的功能和方案完整实现，不拆成过多小轮次。
2. 后续修 Bug 或处理用户建议：每轮对话（每处理一个 Bug 或一条用户建议）都必须遵循最小修改原则，只改与本次问题直接相关的内容，尽量不扩大到其他文件、其他函数或其他模式。

处理第 2 阶段时，必须遵守以下审核包和备份要求：

1. 每轮修改之前，先调用 `ai-change-review` skill，在 `MyCocs/` 下创建新的审核包文件夹；命名规则为 `<第一次大规模实现的审核包名>-<x>`，其中 `<x>` 为本次是第几轮修 Bug / 用户建议修改（例如第 1 次为 `xxx-1`，第 2 次为 `xxx-2`）。一轮对话对应一个文件夹，不要每改一处就新建一个。
2. 第 2 阶段的每轮修改不需要再次迁出 P4 文件；相关文件在第一次大规模实现时已经在 default changelist 中，后续直接在该 changelist 上继续修改即可，不要额外迁出。
3. 使用 `ai-change-review` 备份原始快照时，不要一次性把所有可能涉及的文件都备份：必须先确定本轮要修改哪个文件，再在修改该文件之前立即备份它；禁止在已经修改过文件之后才补备份。未确定要修改的文件不要备份。
4. 如果用户的建议是在保持现有逻辑不变的前提下调整代码设计（例如重构、缩小公共改动、调整父子类职责），不要凭记忆修改。必须先用 `ai-change-review` 的 diff 功能（或对比上一次审核包的 originals 与当前代码）确认上一轮究竟改了什么，再在该 diff 基础上执行本轮建议。
5. 第 2 阶段审核包中的 `docs/用户需求.md` 不是整个模块或第一次大规模实现的完整需求文档，只记录本轮用户提出的 Bug、修改建议或设计要求，并按本轮实际修改范围组织需求层级。`review.json` 的 `requirementRelation` 只能引用本轮需求文档中的编号，不得把上一轮大规模实现的其他需求、未在本轮修改的功能或 Agent 自行推导的目标写入本轮需求。

每轮对话结束之后，
